<?xml version="1.0" encoding="UTF-8"?>
<configuration>
	<appender name="console_log" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>
				[%date{yyyy-MM-dd HH:mm:ss}] %contextName [%thread] [%-5level] [%logger:%line] --%mdc{client} %msg%n
			</pattern>
			<!-- 控制台也要使用UTF-8，不要使用GBK，否则会中文乱码 -->
			<charset>UTF-8</charset>
		</encoder>
		<!--过滤掉所有低于INFO级别的日志-->
		<filter class="ch.qos.logback.classic.filter.ThresholdFilter">
			<level>INFO</level>
		</filter>
		<layout class="ch.qos.logback.classic.PatternLayout">
			<pattern>
				[%date{yyyy-MM-dd HH:mm:ss}] %contextName [%thread] [%-5level] [%logger:%line] --%mdc{client} %msg%n
			</pattern>
		</layout>
	</appender>
    <appender name="file_log" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <File>logs/agile-server.log</File>
        <!-- rollingPolicy:当发生滚动时，决定 RollingFileAppender 的行为，涉及文件移动和重命名。 -->
        <!-- TimeBasedRollingPolicy： 最常用的滚动策略，它根据时间来制定滚动策略，既负责滚动也负责出发滚动 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 活动文件的名字会根据fileNamePattern的值，每隔一段时间改变一次 -->
            <!-- 文件名：log/portal-sso.2017-12-05.0.log -->
            <fileNamePattern>logs/agile-server.%d.%i.log.gz</fileNamePattern>
            <!-- 每产生一个日志文件，该日志文件的保存期限为30天 -->
            <maxHistory>30</maxHistory>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <!-- maxFileSize:这是活动文件的大小，默认值是10MB,本篇设置为1KB，只是为了演示 -->
                <maxFileSize>100MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>
        <encoder>
            <!-- pattern节点，用来设置日志的输入格式 -->
            <pattern>
                [%date{yyyy-MM-dd HH:mm:ss}] %contextName [%thread] [%-5level] [%logger:%line] --%mdc{client} %msg%n
            </pattern>
            <!-- 记录日志的编码 -->
            <charset>UTF-8</charset> <!-- 此处设置字符集 -->
        </encoder>
    </appender>
	<springProperty scope="context" name="graylog.host" source="graylog.host"/>
	<springProperty scope="context" name="graylog.port" source="graylog.port"/>
	<springProperty scope="context" name="graylog.appname" source="graylog.appname"/>
	<springProperty scope="context" name="graylog.appenv" source="graylog.appenv"/>
	<appender name="GELF" class="de.siegmar.logbackgelf.GelfUdpAppender">
		<graylogHost>${graylog.host}</graylogHost>
		<graylogPort>${graylog.port}</graylogPort>
		<maxChunkSize>508</maxChunkSize>
		<useCompression>true</useCompression>
		<encoder class="de.siegmar.logbackgelf.GelfEncoder">
			<includeRawMessage>false</includeRawMessage>
			<includeMarker>true</includeMarker>
			<includeMdcData>true</includeMdcData>
			<includeCallerData>false</includeCallerData>
			<includeRootCauseData>false</includeRootCauseData>
			<includeLevelName>true</includeLevelName>
			<shortPatternLayout class="ch.qos.logback.classic.PatternLayout">
				<pattern>%m%nopex</pattern>
			</shortPatternLayout>
			<fullPatternLayout class="ch.qos.logback.classic.PatternLayout">
				<pattern>%m%n</pattern>
			</fullPatternLayout>
			<numbersAsString>false</numbersAsString>
			<staticField>app_name:${graylog.appname}</staticField>
			<staticField>app_env:${graylog.appenv}</staticField>
		</encoder>
	</appender>
	<logger name="org.springframework.boot.actuate.endpoint.web.servlet" level="trace"/>
	<!-- 控制台输出日志级别 -->
	<root level="INFO">
		<appender-ref ref="console_log" />
        <appender-ref ref="file_log"/>
		<appender-ref ref="GELF"/>
	</root>
</configuration>
